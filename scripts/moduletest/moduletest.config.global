if [ -z "$WORKDIR" ]; then
    WORKDIR=/scratch/`whoami`/test.`date +%s` # default workdir if not specified in environment
fi
OUTPUT_DIR=$TESTSCRIPT_DIR/output # path to output directory
LFILE=$FACTSSCRIPT_DIR/moduletest/location_list.lst # default location file 
SCENARIO=ssp585 # default scenario to run
NSAMPS=500 # default number of samples to produce
